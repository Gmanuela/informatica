// funciones: encapsular funcionalidad y abstracción (no preocuparse de los detalles sino llamar la funciñon)
// crear una función: function mayor (a,b) 
// {if (a>b) return a; 
// else return b; }

function mayor (a,b)
{
    if (a > b) return a; 
    else return b; 
}
let x = 4 
let y = 5
console.log (`el mayor es  ${mayor (x, y)}´); 